#set text(size: 1.8em)


`sum`, `val`

=== Case 1: ( `val > 0` )

count of 0 passes during rot = `(sum + val) / 100`

=== Case 2: ( `val < 0` )

count of 0 passes during rot = `((100 - sum) + (-val)) / 100`
